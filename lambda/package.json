{
  "name": "bedrock-chatbot-lambda",
  "version": "1.0.0",
  "description": "Lambda function for Bedrock Claude 3 Haiku chatbot",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "clean": "rm -rf node_modules dist build cdk.out .cache",
    "build": "...",
    "deploy": "..."
  },
  "keywords": [
    "aws",
    "bedrock",
    "lambda",
    "chatbot"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@aws-sdk/client-bedrock-runtime": "^3.414.0"
  }
}
